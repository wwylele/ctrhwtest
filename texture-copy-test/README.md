TextureCopy test

Usage:
Press A, and input `<w1> <g1> <w2> <g2> <l>`, where:
 - `<w1>` is the input width in bytes / 16
 - `<g1>` is the output gap in bytes / 16
 - `<w2>` is the input width in bytes / 16
 - `<g2>` is the output gap in bytes / 16
 - `<l>` is the total length in bytes
 
